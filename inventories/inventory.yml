all:
  vars:
  children:
    pi_servers:
      vars:
        ansible_user: pi
        ansible_ssh_pass: "{{ pi_ssh_pass }}"
      hosts:
        plex_pi:
          ansible_host: 192.168.1.22
        pi_torrents:
          ansible_host: 192.168.1.32